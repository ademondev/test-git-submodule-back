# Test Git Submodule - Backend

This is the backend API for the test git submodule project.

## Setup

1. Install dependencies: `npm install`
2. Start the server: `npm start`
3. Check documentation in the `docs/` folder (git submodule)

## Features

- RESTful API
- Health check endpoint
- User management
- Comprehensive documentation via git submodule